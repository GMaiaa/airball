import styled, { css } from "styled-components/native";

export const Container = styled.View`
  flex-direction: row;
  align-items: center;
`;

export const StarItem = styled.TouchableOpacity`
  padding: 5px;
`;

export const EmptySpace = styled.View`
  opacity: 0.6;
`;